# 外挂 BLE 模块 · 接线与配置说明

> 版本 v1 ｜ 2026-09-08 ｜ 依据：`剂量仪电路原理图.pdf`（REV 1.0，STM32F103C8T6，U1）
> 配套：`protocol.md`（数据协议）、`firmware/stm32_uart_report.c`（固件上报示例）

## 1. 目的
把外挂 BLE 透传模块接到现有剂量仪板子上，让 STM32 每秒通过串口把测量数据发给模块，模块无线转发给安卓网页（Web Bluetooth 订阅 NUS）。

## 2. 模块选型
- 参考：nRF52832 透传模块（NUS 透传固件），如 **E104-BT5032A** 或同类“nRF52832 BLE UART 透传”模块（3.3V、带板载天线）。
- 要求：透传模式 + 3.3V 逻辑；若换其他模块，只需同步改：
  - 网页 `index.html` 顶部 UUID 常量（非 NUS 时）；
  - 模块的透传/波特率 AT 配置。
- 数据协议不变。

## 3. 接线表（按实际原理图核对）
| 模块引脚 | 接到板上 | 说明 |
|---|---|---|
| VCC | **+3.3V** | 数字 3.3V 轨（TPS7A03 LDO 输出）。⚠️ **不要接 VCC3.3V**：那是 ASIC/比较器模拟轨，会引入噪声 |
| GND | **GND** | 共地；不要接 AGND |
| RXD | **PA9**（USART1_TX，引脚30） | MCU 发送 → 模块接收 |
| TXD | **PA10**（USART1_RX，引脚31） | 模块发送 → MCU 接收 |

- 串口参数：**115200 8N1**（固件与模块需一致）。
- 引脚依据：原理图中 PA9/PA10 **未连接任何网络**，空闲可用；已用引脚（PA12 脉冲、PA6/7/PB8/9 按键、PB5 LED、PB0 蜂鸣器、PB6/7 OLED IIC、PA13/14 SWD）互不冲突。
- 备用串口（若 PA9/PA10 日后被占用）：USART2 = PA2/PA3、USART3 = PB10/PB11，原理图中同样空闲。

## 4. 模块配置步骤（透传）
1. 模块上电（3.3V），串口连电脑 USB-TTL 或直接连板子；
2. 确认/设置模块为**透传模式**、**波特率 115200**、**NUS 服务**（不同模块 AT 指令不同，以模块数据手册为准；多数 nRF52832 透传模块出厂即 NUS 透传，仅需核对波特率）；
3. 用手机 nRF Connect 扫描，应能看到模块广播且包含 NUS 服务（`6e400001-...`）。

## 5. 摆放与电气注意
- 模块与天线放在**远离 OLED 和前端 ASIC 的一端**（论文已发现蜂鸣器/OLED 对 ASIC 有电磁干扰），天线下方避开铜皮；
- 飞线尽量短；模块电源就近加 10~100µF + 100nF 去耦；
- 模块平均电流约 mA 级、发射峰值约 10mA 级：确认板子电池/3.3V 供电余量（原理图有锂电池接口 BAT，但无充电管理）。

## 6. 与固件/网页衔接
- 固件：按 `firmware/stm32_uart_report.c` 初始化 USART1 并每秒调用上报函数；
- 网页：连接仪器 → 订阅 NUS Notify → 解析每秒一条 JSON（见 `protocol.md`）；
- 联调顺序：①USB-TTL 看固件串口输出 → ②模块 + nRF Connect 看 Notify → ③手机网页端到端。
