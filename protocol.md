# 数据协议文档：剂量仪 → 安卓网页

> 版本 v1 ｜ 2026-09-08 ｜ 对应网页 `index.html` 顶部常量
> 用途：规定 STM32 固件经 BLE 透传模块发给手机网页的数据格式，两端按此实现。

## 1. 适用范围
- 仪器端：STM32F103C8T6 固件（Keil5 / 标准外设库）
- 传输：外挂 BLE 透传模块（NUS 透传），STM32 USART1 → 模块 → 手机
- 接收端：安卓 Chrome/Edge 网页（Web Bluetooth）

## 2. 通信链路与参数
| 项 | 值 |
|---|---|
| 蓝牙服务 | Nordic UART Service (NUS) `6e400001-b5a3-f393-e0a9-e50e24dcca9e` |
| Notify 特征（设备→手机） | `6e400003-b5a3-f393-e0a9-e50e24dcca9e` |
| 网页是否写数据 | 否（仅订阅 Notify） |
| STM32 串口 | USART1：PA9(TX)→模块RX，PA10(RX)←模块TX；115200 8N1 |
| 发送频率 | 每秒 1 条 |
| 编码 | UTF-8 文本，每条约 100~200 字节，以换行符 `\n`(0x0A) 结尾 |

## 3. 消息格式
每条消息是**一个完整的 JSON 对象 + 一个 `\n`**。类型用 `t` 区分，当前只有剂量类型 `d`。

### 3.1 剂量消息（每秒发送）
```json
{"t":"d","cps":123,"rate":0.12,"acc":0.34,"thr":0.50,"al":0}
```
| 字段 | 类型 | 必填 | 含义 | 单位 |
|---|---|---|---|---|
| `t` | string | 是 | 消息类型，固定 `"d"` | - |
| `cps` | number | 是 | 每秒计数率（整数） | 个/秒 |
| `rate` | number | 否* | 剂量率 | µSv/h |
| `acc` | number | 否* | 累计剂量 | µSv |
| `thr` | number | 否* | 当前报警阈值 | µSv/h |
| `al` | number | 否* | 报警状态：0 正常 / 1 超阈值 | - |

*建议每次都发全 5 个字段（rate/acc/thr/al 取固件当前用于 OLED 显示/报警判定的实际变量值）。
网页对缺失字段显示 `--`；`cps` 与 `rate` 至少出现一个，该条才会被接受并刷新界面。

## 4. 接收端解析规则（网页已实现，勿改）
1. BLE 可能拆包：接收端把字节追加到缓冲区，**按 `\n` 切出完整行**；
2. 每行先 `trim`，空行丢弃；
3. 对整行做 `JSON.parse`，失败（坏行/半行）直接丢弃，不影响下一条；
4. `al` 缺省时：由网页按 `rate >= thr` 自行判定报警；
5. `al=1` 时网页触发整套报警（整页红闪 + 大数字发光 + 横幅 + 震动）。

## 5. 固件端发送要求（STM32）
- 用标准库初始化 USART1（PA9/PA10，115200 8N1，TX 推挽复用）；
- 在每秒一次的计数/显示逻辑里调用一个上报函数，发送格式示例（C 语言）：
```c
// g_cps/g_rate/g_acc/g_thr/g_alarm 需映射为你工程里 OLED 显示/报警用的实际变量
char buf[160];
snprintf(buf, sizeof(buf),
    "{\"t\":\"d\",\"cps\":%lu,\"rate\":%.3f,\"acc\":%.3f,\"thr\":%.3f,\"al\":%d}\r\n",
    (unsigned long)g_cps, g_rate, g_acc, g_thr, g_alarm);
// 逐字节或按串口发送 buf（注意结尾用 \n）
```
- `snprintf` 需包含 `<stdio.h>` 并开启微库（MicroLib）或在工程里提供重定向；发送可用阻塞式逐字节或 DMA，按你现有代码风格选择。
- 注意：文档用 `\r\n` 或 `\n` 均可，网页只按 `\n` 分行，行首行尾多余空白会被忽略。

## 6. 单位与标定说明
- 单位默认：剂量率/阈值 µSv/h，累计剂量 µSv，计数率 个/秒。
- 论文中剂量率**尚未做剂量标定**：固件 `rate` 请如实发送当前显示值；网页会显示“数值未标定，仅供参考”。若将来完成标定，把网页常量 `CALIBRATED` 改为 `true` 即可隐藏提示。
- 若固件单位与上述不同，需同步修改网页顶部常量（`RATE_UNIT` 等）与本文档。

## 7. 完整示例（1 秒内的实际输出）
```
{"t":"d","cps":156,"rate":0.28,"acc":1.234,"thr":0.50,"al":0}
{"t":"d","cps":170,"rate":0.31,"acc":1.235,"thr":0.50,"al":0}
{"t":"d","cps":523,"rate":0.94,"acc":1.236,"thr":0.50,"al":1}
```

## 8. 变更记录
- v1（2026-09-08）：初版，定义剂量消息 `t=d` 及字段/单位/解析规则。
