# 外挂 BLE 模块 · 飞线接线记录

| 项目 | 内容 |
|---|---|
| 文档版本 | v1 |
| 日期 | 2026-09-10 |
| 适用板卡 | 小尺寸便携式剂量仪（原理图 REV 1.0，STM32F103C8T6） |
| 变更性质 | **不修改原理图**：BLE 模块为板外器件，采用飞线连接（样机方案） |
| 关联文档 | `protocol.md`（数据协议）、`hardware_ble_wiring.md`（模块选型/配置）、`firmware/keil_dose_app/`（固件） |

## 1. 为什么原理图不需要改动
BLE 透传模块是**板外组件**，只用 4 根飞线接到现有电路（电源、地、PA9、PA10），板上**没有新增器件、没有新增走线**，因此主原理图在电气上保持原样是正确的。

> 本记录的作用：弥补主图"看不出这 4 根飞线"的问题，作为样机改动说明，可用于调试、交接与论文/报告。

## 2. 接线表（模块 → 板子）

| BLE 模块引脚 | 焊接到板子 | 说明 |
|---|---|---|
| VCC | **+3.3V**（数字轨） | 推荐取电点：OLED 座第 2 脚（VCC）；备选：SWD 排针 +3.3V、H1 BOOT 排针 +3.3V |
| GND | **GND** | 推荐取电点：OLED 座第 1 脚（GND）；备选：SWD 排针 GND |
| TXD | STM32 **第 31 脚 = PA10**（USART1_RX） | 模块发 → MCU 收 |
| RXD | STM32 **第 30 脚 = PA9**（USART1_TX） | MCU 发 → 模块收 |
| EN/RST（若模块有） | 按模块手册处理 | 多数模块内部已上拉，可悬空 |
| 去耦电容 | 100nF + 10µF 并联在模块 VCC–GND | 尽量靠近模块焊盘 |

**⚠️ 必须交叉连接**：模块 TXD → MCU 的 RX(PA10)；模块 RXD → MCU 的 TX(PA9)。
串口参数：**115200 8N1**（与 `protocol.md`、固件一致）。

## 3. 取电点说明（三选一）
1. **OLED 座（推荐）**：原理图中 OLED1 为 4 脚 `1=GND、2=VCC、3=SCL、4=SDA`，是现成焊盘，好焊、就近取电；
2. **SWD 排针**：含 `+3.3V` 与 `GND`（按丝印辨认）；
3. **H1 BOOT 排针（1×3）**：含 +3.3V 与 GND，可取电，**严禁动 BOOT0 那根**。

**电气红线**：
- 只能用 **+3.3V（数字轨）**，**禁止接 VCC3.3V**（ASIC/比较器模拟轨）；
- 地只能用 **GND**，**禁止接 AGND**；
- 模块必须是 **3.3V 逻辑**（不可用 5V 模块直连）。

## 4. 焊接与布局要求
- 数据线接 **STM32F103C8T6（LQFP48）第 30/31 脚**，这两脚在原理图上为空、无焊盘，只能直接焊芯片引脚；
- 用 **0.1~0.2mm 漆包线**；先给引脚上锡，快速点焊；烙铁约 300°C + 助焊剂 + 放大镜/显微镜，避免烫坏芯片、避免相邻脚短路；
- 焊后用 **UV 胶/热熔胶**固定，做应力释放，防止扯断引脚；
- 模块放在板子**远离前端 ASIC 和 OLED 的一端**（论文已发现 OLED/蜂鸣器对 ASIC 有干扰）；
- 天线区域**下方与正前方不要有铜皮/金属**，尽量让天线悬出板边；飞线尽量短。

## 5. 与固件/网页的对应
- 固件：`firmware/keil_dose_app/ble_report.c`，`USART1`，PA9=TX、PA10=RX，每秒发一行 JSON；
- 协议：见 `protocol.md`，形如
  `{"t":"d","cps":123,"rate":0.120,"acc":0.340,"thr":0.500,"al":0}`；
- 网页：https://cao-hue.github.io/dose-mobile/ → "连接仪器" → 实时显示。

## 6. 上电与验收步骤
1. **先不接模块测电压**：用万用表确认取电点对 GND 为 3.3V；
2. 焊好 VCC/GND，上电测模块供电正常（不发热、不异常电流）；
3. 用 USB-TTL 接 PA9 观察：应每秒收到一行 JSON（验证固件）；
4. 接模块 TXD 到 PA10 后：手机 nRF Connect 应能看到 Notify 数据；
5. 手机网页端到端：数值与 OLED 显示一致，超阈值时 LED/蜂鸣器与网页报警联动。

## 7. 注意事项
- PA9/PA10 同时是 STM32 **USART1 串口下载脚**：若用 BOOT0=1 串口下载，建议先断开模块 TXD，或统一使用 **SWD** 下载（本项目默认 SWD，不受影响）；
- 焊接前先做**防静电**（洗手/防静电手环），避免损坏 ASIC 与 MCU；
- 飞线样机**不适合长期使用**（振动、可靠性），正式产品应在下一版 PCB 中集成 BLE 模块（可加 4 脚排针或板载模块）。

## 8. 变更记录
| 版本 | 日期 | 内容 | 主原理图 |
|---|---|---|---|
| v1 | 2026-09-10 | 初版：外挂 BLE 飞线接线记录 | 无改动 |
