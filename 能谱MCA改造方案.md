# 能谱（MCA）改造方案 + 改图工单

> 目标：在改进版剂量仪上新增能谱读出通道（不改动原有计数/剂量/OLED/按键/LED/蓝牙功能）
> 依据：NEWSTAR2 规格书（内部资料）+ 改进版网表 `Netlist_剂量仪-最新_2026-09-11.asc`
> 结论：ASIC 的 `PDHOUT`(脚1)、`RSTPDH`(脚14) 原图未连接；`TRIG`(脚18) 已用于计数（→R22→SN74LV1T34→PA0）

## 一、新增器件清单（虚线框内为新增）
| 位号 | 型号/参数 | 封装 | 作用 |
|---|---|---|---|
| U12 | **AD8531ARTZ**（与板上 U6 同型号） | SOT23-5 | PDHOUT 电压跟随器（缓冲） |
| R33 | 1 kΩ | 0603 | ADC 前串联限流/隔离 |
| C44 | 100 pF | 0603 | 与 R33 组成 RC 滤波（τ≈0.1µs，远小于 5µs 读取延时） |
| C45 | 100 nF | 0603 | U12 电源去耦 |
| C46 | 10 µF | 0805 | U12 电源去耦 |
| R34 | 100 Ω | 0603 | RSTPDH 串联（可选） |
| R35 | 100 Ω | 0603 | 触发信号串联（可选） |
| TP10~TP12 | 测试点 | — | MCA_ADC / RSTPDH / MCA_TRIG 调试用 |
| D4 | BAT54S（可选） | SOT23 | ADC 输入端钳位保护 |

## 二、连线工单（照此在 LCEDA 里连线）
| 序号 | 从 | 经过 | 到 | 说明 |
|---|---|---|---|---|
| 1 | U4.1（PDHOUT，ASIC 峰保输出） | U12 同相输入(+) | — | 高阻输入，不加载 ASIC |
| 2 | U12 输出 | 与 U12 反相输入(−) 短接 | — | 构成电压跟随器 |
| 3 | U12 输出 | R33(1k) → 节点 MCA_ADC | — | 串联隔离 |
| 4 | MCA_ADC | C44(100pF) → AGND | — | RC 滤波 |
| 5 | MCA_ADC | — | **U2.18 = PB0（ADC1_IN8）** | ADC 采样输入 |
| 6 | U4.14（RSTPDH） | R34(100Ω) | **U2.21 = PB10（GPIO 输出）** | 1=复位，0=PDH工作 |
| 7 | U8.4（已电平变换的 TRIG，与 PA0 同一网络） | R35(100Ω) | **U2.22 = PB11（EXTI 上升沿）** | 能谱触发（PA0 计数不受影响） |
| 8 | U12 供电 | VCC3.3V + AGND | C45/C46 就近去耦 | 模拟电源，勿用 +3.3V 数字轨 |
| 9 | MCA_ADC / RSTPDH / MCA_TRIG | TP10~TP12 | — | 调试测试点 |

> ⚠️ 关键：PDHOUT 电压范围（基线 1.258V，满量程≈2.76V）在 ADC 的 0~3.3V 内；`RSTPDH=1 复位、0 工作`，写反会读不到峰值。

## 三、固件实现（新增 mca.c/.h，集成进 keil_dose_app_v2）
1. **时序**：PB11 上升沿中断 → 延时 ≥5µs（DWT）→ 读 PB0 ADC（55.5 周期采样）→ 算道址 → `hist[ch]++` → `RSTPDH=1`（约 1µs）→ `RSTPDH=0` → 等待下次触发。
2. **道址换算**：`ch = (adc - BASELINE) * 1024 / (FULLSCALE - BASELINE)`，越界丢弃；BASELINE/FULLSCALE 为标定常量（初值对应 1.258V≈1561、2.76V≈3425 ADC 码）。
3. **堆积/死时间**：处理期间若再次触发（EXTI pending）计入 pile-up 并丢弃；维护 live_time/dead_time。
4. **直方图**：内部 `uint32_t hist[1024]`（4KB RAM）；每 5 秒把 4 道合并为 1 道 → 256 道，分 4 条 JSON（每条 64 道）上传：
   `{"t":"s","n":256,"i":0,"ch":[...]}`
5. **兼容**：`MCA_ENABLE` 宏可整体关闭；关闭时剂量功能不受影响。

## 四、网页与标定
- 网页解析 `"t":"s"` 分片，拼成 256 道直方图，新增"能谱视图"（对数纵轴、峰值标注），与剂量视图切换；沿用 0xFFE0/0xFFE2。
- 标定：无源测基线；用 ²⁴¹Am(59.5keV)、¹³⁷Cs(662keV)、⁶⁰Co(1173/1332keV) 找峰位，线性拟合 `E = a·ch + b`。
- 指标：光电峰 FWHM、能量分辨率 `R=FWHM/E×100%`、线性度、峰位稳定性、不同计数率的死时间影响。

## 五、测试计划
1. 示波器：PDHOUT 基线≈1.258V、有源时峰值随能量上升；PB11 触发与 PA0 计数同步。
2. 直方图：²⁴¹Am 出现 59.5keV 峰；¹³⁷Cs 出现 662keV 峰；升计数率观察堆积。
3. 并行验证：能谱运行时，CPS/剂量/阈值 LED 报警必须正常。
4. 端到端：手机网页 5 秒内收到一帧 256 道谱并正确绘出；断连重连可继续。
5. 分辨率：按标定峰算 FWHM，与手册 ENC（180 e⁻）理论值对比。

## 六、假设与默认值
- ADC：STM32 内置 12 位（PB0/ADC1_IN8），不装外置 ADC。
- 触发：PB11 EXTI（源自 U8.4 已电平变换的 TRIG）；PA0/TIM2 计数不变。
- RSTPDH：PB10，1=复位/0=工作。
- 上传：每 5 秒一帧、256 道、每条 64 道分 4 条 JSON。
- 缓冲：AD8531（与板上 U6 同型号，便于 BOM 统一）。
- 12 位 ADC：1 LSB≈0.81mV≈0.032fC，与 ASIC ENC≈0.029fC 同量级，分辨率以 ASIC 噪声+ADC 量化为主。

## 七、能量区间与道宽（已确定）
- **能量区间：30 ~ 1600 keV**（覆盖 ²⁴¹Am 59.5、¹³⁷Cs 662、⁶⁰Co 1173/1332、⁴⁰K 1461）
- 256 道 → **6.13 keV/道**；内部 1024 道 → 1.53 keV/道
- 换算依据：1 fC ≈ 28.7 keV（CdZnTe 4.6 eV/电子空穴对）、ASIC 增益 25 mV/fC、
  PDHOUT 基线 1.258V、ADC 3.3V/4096
- 固件常量：`MCA_ADC_BASE = 1594`（30 keV）、`MCA_ADC_FULL = 3292`（1600 keV）
- 低于 30 keV 或高于 1600 keV 的事件计入 `outrange`，不落道
- 注意：以上为标称值换算，精确能量刻度需用标准源标定
